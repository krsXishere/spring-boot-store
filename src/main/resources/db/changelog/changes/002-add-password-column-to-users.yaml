databaseChangeLog:
  - changeSet:
      id: 002-add-password-column-to-users
      author: ikis

      changes:
        - addColumn:
            tableName: users
            columns:
              - column:
                  name: password
                  type: varchar(255)

      rollback:
        - dropColumn:
            tableName: users
            columnName: password
